@model IEnumerable<KabanBoard.Models.POCOs.ItemNames>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ItemNameTitle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ItemNameTitle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ItemNamesID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ItemNamesID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ItemNamesID })
        </td>
    </tr>
}

</table>
