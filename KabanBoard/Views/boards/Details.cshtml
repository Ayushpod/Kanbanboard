@model KabanBoard.Models.POCOs.boards

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>boards</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Title)
            @foreach(var iteeem in Model.Items)
            {
                <td>
                    <div>
                    Column :   <h5> @iteeem.ItemNameTitle </h5>
                    </div>
                </td>
            }
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.boardID }) |
    @Html.ActionLink("Back to List", "Index")|
    @Html.ActionLink("Add", "Create","ItemNames",new { id = Model.boardID },null) 
</p>
